N = len(a)
    # x_new = [0 for _ in range(N)]
    # for i in range(N):
    #     x_new[i] = b[i]
    #     for j in range(N):
    #         if i != j:
    #             x_new[i] -= a[i][j] * x0[j]
    #     x_new[i] /= a[i][i]
    # return x_new